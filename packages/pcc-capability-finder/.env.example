# TinyFish-powered demand-side agent. Only 3 keys needed.
TINYFISH_API_KEY=                # agent.tinyfish.ai/sign-up (500 cr/mo free)
PCC_AGENT_PACKAGE_URL=https://capability.network/agent-package.json
ANTHROPIC_API_KEY=               # optional — used to rank candidates if set

# Optional: cross-reference x402 quotes via CDP facilitator
CDP_API_KEY_ID=
CDP_API_KEY_SECRET=
